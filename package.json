{
  "name": "reps",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm --stream --filter=client --filter=server run dev",
    "build": "pnpm --filter server run dev & pnpm --filter client run build && pnpm --filter common run build && pnpm --filter server run build",
    "serve": "pnpm --parallel --stream --filter client --filter server run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^9.1.0",
    "globals": "^15.0.0",
    "prettier": "^3.1.1",
    "typescript": "^5.6.2"
  }
}
