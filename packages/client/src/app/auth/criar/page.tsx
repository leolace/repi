import { Button } from "@components/button";

const Create = () => {
  const handleClick = () => {
    console.log("oi");
  };

  return (
    <div>
      <Button>Botao</Button>
    </div>
  );
};

export default Create;
